name: GitHub Actions Demo

on:
  schedule:
    - cron: "0 * * * *" # Every Hour
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  Schedule:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Lock branch
        id: lock
        uses: ./
        with:
          token: ${{ secrets.LOCK_TOKEN }}
          owner: JustinDFuller
          repository: github-action-lock-branch
          branch: main
          lock: true

      - name: UnLock branch
        id: unlock
        uses: ./
        with:
          token: ${{ secrets.LOCK_TOKEN }}
          lock: false

      - run: echo "${{ steps.lock.outputs.locked }}"
      - run: echo "${{ steps.lock.outputs.unlocked }}"
      - run: echo "${{ steps.lock.outputs.changed }}"
      - run: echo "${{ steps.lock.outputs.success }}"
      - run: echo "${{ steps.lock.outputs.failure }}"
      - run: echo "${{ steps.lock.outputs.owner }}"
      - run: echo "${{ steps.lock.outputs.repository }}"
      - run: echo "${{ steps.lock.outputs.branch }}"

      - run: echo "${{ stepa.unlock.outputs.locked }}"
      - run: echo "${{ stepa.unlock.outputs.unlocked }}"
      - run: echo "${{ stepa.unlock.outputs.changed }}"
      - run: echo "${{ stepa.unlock.outputs.success }}"
      - run: echo "${{ stepa.unlock.outputs.failure }}"
      - run: echo "${{ stepa.unlock.outputs.owner }}"
      - run: echo "${{ stepa.unlock.outputs.repository }}"
      - run: echo "${{ stepa.unlock.outputs.branch }}"
